# =====================================================================
# Şeffaf Bağış Platformu - Tüm Ortamlar Örnek Configuration
# =====================================================================

# Backend (.env) dosyasını oluşturmak için backend klasöründen:
# cp backend/.env.example backend/.env
# Ardından backend/.env dosyasını düzenleyin

# Frontend (.env.local) dosyasını oluşturmak için frontend klasöründen:
# cp frontend/.env.example frontend/.env.local
# Ardından frontend/.env.local dosyasını düzenleyin

# Docker Compose ortamı için:
# docker-compose -f docker/docker-compose.yml up -d
